import path from 'path';

const paths = {
    src: path.join(__dirname, '../src/'),
    dist: path.join(__dirname, '../dist/'),
    test_dist: path.join(__dirname, '../tests/dist/')
};

export default paths;
